syntax = "proto3";

package senseye.compute;
import "common.proto";

service Compute {
    rpc AnalyzeVideoStream(stream senseye.VideoStreamRequest)
        returns (stream senseye.VideoStreamResponse);

    rpc AnalyzeVideoStatic(senseye.VideoStaticRequest)
        returns (stream senseye.VideoStreamResponse);
}
