syntax = "proto3";

package senseye.gateway;

import "common.proto";

service Gateway {
    rpc AnalyzeVideoStream(stream senseye.VideoStreamRequest)
        returns (stream senseye.VideoStreamResponse);

    rpc AnalyzeVideo(senseye.VideoStaticRequest)
        returns (stream senseye.VideoStreamResponse);
}
